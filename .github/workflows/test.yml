on: [push, pull_request]
name: Test
jobs:
  test:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Install Go
      uses: actions/setup-go@v6
      with:
        go-version: stable
    - name: Checkout code
      uses: actions/checkout@v5
    - name: Build
      run: go build ./...
